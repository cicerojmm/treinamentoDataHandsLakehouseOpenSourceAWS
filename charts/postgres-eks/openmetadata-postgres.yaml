apiVersion: v1
kind: Secret
metadata:
  name: openmetadata-postgres-secret
  namespace: governance
type: Opaque
stringData:
  POSTGRES_USER: openmetadata
  POSTGRES_PASSWORD: openmetadata123
  POSTGRES_DB: openmetadata
---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: openmetadata-postgres-pvc
  namespace: governance
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: gp3
  resources:
    requests:
      storage: 10Gi
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: openmetadata-postgres
  namespace: governance
spec:
  replicas: 1
  selector:
    matchLabels:
      app: openmetadata-postgres
  template:
    metadata:
      labels:
        app: openmetadata-postgres
    spec:
      containers:
        - name: postgres
          image: postgres:15-alpine
          ports:
            - containerPort: 5432
          envFrom:
            - secretRef:
                name: openmetadata-postgres-secret
          env:
            - name: PGDATA
              value: /var/lib/postgresql/data/pgdata
          volumeMounts:
            - name: data
              mountPath: /var/lib/postgresql/data
          resources:
            requests:
              cpu: 100m
              memory: 256Mi
            limits:
              cpu: 500m
              memory: 512Mi
      volumes:
        - name: data
          persistentVolumeClaim:
            claimName: openmetadata-postgres-pvc
---
apiVersion: v1
kind: Service
metadata:
  name: openmetadata-postgres
  namespace: governance
spec:
  selector:
    app: openmetadata-postgres
  ports:
    - port: 5432
      targetPort: 5432
