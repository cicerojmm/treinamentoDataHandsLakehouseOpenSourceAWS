apiVersion: v1
kind: Secret
metadata:
  name: hive-metastore-postgres-secret
  namespace: data-platform
type: Opaque
stringData:
  POSTGRES_USER: hive
  POSTGRES_PASSWORD: hive123
  POSTGRES_DB: metastore
---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: hive-metastore-postgres-pvc
  namespace: data-platform
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: gp3
  resources:
    requests:
      storage: 10Gi
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: hive-metastore-postgres
  namespace: data-platform
spec:
  replicas: 1
  selector:
    matchLabels:
      app: hive-metastore-postgres
  template:
    metadata:
      labels:
        app: hive-metastore-postgres
    spec:
      containers:
        - name: postgres
          image: postgres:15-alpine
          ports:
            - containerPort: 5432
          envFrom:
            - secretRef:
                name: hive-metastore-postgres-secret
          env:
            - name: PGDATA
              value: /var/lib/postgresql/data/pgdata
          volumeMounts:
            - name: data
              mountPath: /var/lib/postgresql/data
          resources:
            requests:
              cpu: 100m
              memory: 256Mi
            limits:
              cpu: 500m
              memory: 512Mi
      volumes:
        - name: data
          persistentVolumeClaim:
            claimName: hive-metastore-postgres-pvc
---
apiVersion: v1
kind: Service
metadata:
  name: hive-metastore-postgres
  namespace: data-platform
spec:
  selector:
    app: hive-metastore-postgres
  ports:
    - port: 5432
      targetPort: 5432
