apiVersion: v1
kind: Service
metadata:
  name: hive-metastore
  namespace: data-platform
  labels:
    app: hive-metastore
spec:
  type: ClusterIP
  ports:
    - port: 9083
      targetPort: 9083
      protocol: TCP
      name: thrift
  selector:
    app: hive-metastore
