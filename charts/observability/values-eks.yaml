# Overrides do kube-prometheus-stack para EKS (aplicado sobre values-local.yaml)
grafana:
  service:
    type: LoadBalancer
    port: 3001
    annotations:
      service.beta.kubernetes.io/aws-load-balancer-type: nlb
      service.beta.kubernetes.io/aws-load-balancer-scheme: internet-facing
